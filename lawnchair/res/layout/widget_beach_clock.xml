<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_beach_clock_background"
    android:clipToOutline="true"
    android:padding="16dp">

    <!-- Analog clock backdrop -->
    <AnalogClock
        android:id="@+id/beach_clock_analog"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_gravity="center"
        android:alpha="0.30"
        android:dial="@drawable/ic_clock_dial"
        android:hand_hour="@drawable/ic_clock_hand_hour"
        android:hand_minute="@drawable/ic_clock_hand_minute" />

    <!-- Digital overlay: date + time stacked -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <TextClock
            android:id="@+id/beach_clock_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:format12Hour="EEE, MMM d"
            android:format24Hour="EEE, MMM d"
            android:textColor="#B0C9B8D8"
            android:textSize="14sp"
            android:letterSpacing="0.05"
            android:fontFamily="sans-serif-light" />

        <TextClock
            android:id="@+id/beach_clock_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:format12Hour="hh:mm"
            android:format24Hour="HH:mm"
            android:textColor="#C9B8D8"
            android:textSize="64sp"
            android:fontFamily="sans-serif-thin"
            android:includeFontPadding="false" />

    </LinearLayout>

</FrameLayout>
